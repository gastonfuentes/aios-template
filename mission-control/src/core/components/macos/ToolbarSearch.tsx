/**
 * PRP-034 Sub-fase 5: ToolbarSearch ahora es un button trigger real del
 * rightbar de búsqueda federada (antes era `<label>` placeholder vacío).
 * Cmd+K / Ctrl+K globalmente lo dispara también.
 */
export { SearchTrigger as ToolbarSearch } from '@/features/search/components/SearchTrigger'
